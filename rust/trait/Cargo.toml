[package]
name = "trait"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Freestanding Rust bindings for the Trait OS native ABI v1"

[lib]
path = "src/lib.rs"

[features]
default = []
