[package]
name = "openrfs"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Freestanding Rust bindings for the OpenRFS native ABI v1"

[lib]
path = "src/lib.rs"

[features]
default = []
