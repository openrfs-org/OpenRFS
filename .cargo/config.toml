# OpenRFS uses pinned, local Rust crate sources for reproducible offline builds.
[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor/rust-crates"

[net]
offline = true
